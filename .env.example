# Copy to .env for local development. Never commit the real .env (it's gitignored).
# Get this connection string from your Neon project dashboard — Connection Details,
# "Pooled connection", include ?sslmode=require.
DATABASE_URL=postgresql://user:password@ep-example-12345.us-east-2.aws.neon.tech/fpl_tracker?sslmode=require

# Optional: concurrent element-summary requests during the per-player fetch step.
# ELEMENT_SUMMARY_MAX_WORKERS=5
