# Copy to .env for local development. Never commit the real .env.
DATABASE_URL=postgresql://user:password@ep-example-12345.us-east-2.aws.neon.tech/fpl_tracker?sslmode=require

# Comma-separated frontend origins allowed by CORS (localhost:3000 is
# always allowed regardless of this setting).
FRONTEND_ORIGINS=http://localhost:3000
