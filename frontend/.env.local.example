# Copy to .env.local for local development.
# Points at the backend FastAPI app — run it locally (see backend/README or
# `uvicorn app.main:app --reload` from backend/) or point at its real Vercel
# deployment URL.
API_URL=http://localhost:8000
